#ifndef SMOOTH3D_MACHINE_TYPES_H
#define SMOOTH3D_MACHINE_TYPES_H

// This header was manufactured in the smooth3d project specifically for magix3d

// smooth3d can be called from C code so we do not use cstdint
#include <stdint.h>

typedef int64_t int_type;
typedef double float_type;

#endif  // SMOOTH3D_MACHINE_TYPES_H
